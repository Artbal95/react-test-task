import {Box, ChakraProps} from "@chakra-ui/react"

interface IHomeProps extends ChakraProps {
    [key: string]: any
}

const Home = (props: IHomeProps): JSX.Element => {
    const {} = props
    return (
         <Box>

         </Box>
    );
};

export default Home;